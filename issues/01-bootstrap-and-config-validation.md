## 概要
MVPの起動に必要な土台を実装する。

## 対応タスク
- T001 プロジェクト初期化
- T002 CLIエントリ作成
- T003 終了コード定義
- T004 設定スキーマ定義
- T005 設定ロード実装
- T006 設定バリデーション実装
- T007 `sar validate` 実装

## 完了条件
- `sar validate -c <config>` が成功/失敗を終了コードで返す
- エラー時に項目パス付きで表示される
- `npm run build`, `npm test`, `npm run e2e:min` が成功する
